<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Screen capture runs inside a foreground service with the mediaProjection type, which is
         what makes Android show the persistent capture notification (spec section 9). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- The floating recommendation is drawn over Uber (spec section 24). Granted by the user in
         Android settings, never assumed. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Used only for subscription entitlement verification. No screen content is ever sent
         anywhere (spec sections 11, 39 and 51). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="com.android.vending.BILLING" />

    <!--
      Package visibility for the single "Open Uber Driver" button on the Home screen
      (spec section 36). Declared so getLaunchIntentForPackage can see it on Android 11+.
      This app never talks to Uber beyond starting its launcher activity.
    -->
    <queries>
        <package android:name="com.ubercab.driver" />
    </queries>

    <application
        android:name=".TripAssistantApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TripAssistant">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|uiMode|fontScale"
            android:theme="@style/Theme.TripAssistant">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.AssistantService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

    </application>
</manifest>
